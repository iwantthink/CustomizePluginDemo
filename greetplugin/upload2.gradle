apply plugin:'maven'

repositories {
    mavenCentral()
}
group='com.hypers'
version='1.0.0'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}